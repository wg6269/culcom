<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.culcom.mapper.CustomerQueryMapper">

    <sql id="searchCondition">
        WHERE c.branch_seq = #{branchSeq}
        <if test="keyword != null and keyword != ''">
            <choose>
                <when test="searchType == 'phone'">
                    AND c.phone_number LIKE CONCAT('%', #{keyword}, '%')
                </when>
                <otherwise>
                    AND c.name LIKE CONCAT('%', #{keyword}, '%')
                </otherwise>
            </choose>
        </if>
        <if test="filter == 'new'">
            AND c.status IN ('신규', '진행중')
        </if>
    </sql>

    <select id="search" resultType="com.culcom.dto.customer.CustomerResponse">
        SELECT
            c.seq,
            c.name,
            c.phone_number AS phoneNumber,
            c.comment,
            c.commercial_name AS commercialName,
            c.ad_source AS adSource,
            c.call_count AS callCount,
            c.status,
            c.created_date AS createdDate,
            c.last_update_date AS lastUpdateDate,
            CASE WHEN EXISTS (
                SELECT 1 FROM caller_selection_history h
                WHERE h.customer_id = c.seq
                  AND h.selected_date >= #{recentCutoff}
            ) THEN 1 ELSE 0 END AS recentlyCalled
        FROM customers c
        <include refid="searchCondition"/>
        ORDER BY c.created_date DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM customers c
        <include refid="searchCondition"/>
    </select>

</mapper>
